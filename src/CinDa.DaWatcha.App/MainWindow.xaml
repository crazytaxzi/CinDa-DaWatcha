<Window x:Class="CinDa.DaWatcha.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CinDa-DaWatcha" Height="760" Width="1180"
        MinHeight="620" MinWidth="960"
        Background="#111318" Foreground="#F2F4F8"
        Loaded="OnLoaded" Closed="OnClosed">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#1B1F27"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#FF5F7A"/>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Background" Value="#2B303B"/>
            <Setter Property="Foreground" Value="#F2F4F8"/>
            <Setter Property="BorderBrush" Value="#3D4452"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0E1014"/>
            <Setter Property="Foreground" Value="#F2F4F8"/>
            <Setter Property="BorderBrush" Value="#3D4452"/>
            <Setter Property="Padding" Value="8"/>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="CinDa-DaWatcha"
                       FontSize="28" FontWeight="Bold"/>
            <TextBlock Text="PID completion watcher and manual ChatGPT handoff controller"
                       Foreground="#9CA5B5" FontSize="13"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Watch file"
                       VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="ConfigPathBox" Grid.Column="1"/>
            <Button Grid.Column="2" Content="Browse…" Click="OnBrowse"/>
            <Button Grid.Column="3" Content="Reload" Click="OnReload"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="StartButton" Content="Start monitoring"
                    Background="{StaticResource AccentBrush}" Click="OnStart"/>
            <Button x:Name="PauseButton" Content="Pause"
                    IsEnabled="False" Click="OnPause"/>
            <Button Content="Open managed Firefox" Click="OnOpenManagedFirefox"/>
            <Button Content="Open selected chat" Click="OnOpenSelectedChat"/>
            <Button x:Name="ConfirmButton" Content="Confirm message sent"
                    IsEnabled="False" Click="OnConfirmSent"/>
        </StackPanel>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.2*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource PanelBrush}"
                    CornerRadius="6" Padding="8">
                <DataGrid x:Name="WatchGrid" AutoGenerateColumns="False"
                          IsReadOnly="True" SelectionMode="Single"
                          Background="#1B1F27" Foreground="#F2F4F8"
                          RowBackground="#1B1F27"
                          AlternatingRowBackground="#20252E"
                          GridLinesVisibility="Horizontal"
                          BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Watch" Binding="{Binding Id}" Width="1.3*"/>
                        <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="70"/>
                        <DataGridTextColumn Header="Process" Binding="{Binding ProcessName}" Width="1.2*"/>
                        <DataGridTextColumn Header="UUID" Binding="{Binding ShortUuid}" Width="1.2*"/>
                        <DataGridTextColumn Header="State" Binding="{Binding State}" Width="110"/>
                        <DataGridTextColumn Header="Detail" Binding="{Binding Detail}" Width="2*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <GridSplitter Grid.Column="1" Width="8" HorizontalAlignment="Stretch"
                          Background="#111318"/>

            <Border Grid.Column="2" Background="{StaticResource PanelBrush}"
                    CornerRadius="6" Padding="10">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Event log"
                               FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <ListBox x:Name="LogList" Background="#0E1014"
                             Foreground="#C8CED8" BorderThickness="0"
                             FontFamily="Consolas" FontSize="11"/>
                </DockPanel>
            </Border>
        </Grid>

        <Border Grid.Row="4" Margin="0,10,0,0"
                Background="#1B1F27" CornerRadius="6" Padding="10">
            <DockPanel>
                <TextBlock x:Name="StatusText" Text="Ready."
                           Foreground="#D5DAE2"/>
                <TextBlock x:Name="QueueText" DockPanel.Dock="Right"
                           Text="Pending: 0" Foreground="#9CA5B5"/>
            </DockPanel>
        </Border>
    </Grid>
</Window>
